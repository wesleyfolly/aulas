# Banco de códigos e Bibliotecas

- Bibliotecas
    - Instalação de bibliotecas
        
        > **algumas bibliotecas são **oficiais**, mas outras não. Temos que ter cuidado com o código que utilizamos.*
        > 
        
        Como instalar bibliotecas em python?
        
        `pip install nome-de-algum-pacote`
        
        `pip install Numpy`
        
        onde eu executo isso? Na linha de comando. **cmd** do windows por exemplo.
        
    - urllib (manipulação de URL)
        
        O módulo Urllib é o módulo de manipulação de URL para python. É usado para buscar URLs (Uniform Resource Locators). Ele usa a função *urlopen* e é capaz de buscar URLs usando uma variedade de protocolos diferentes.
        
    - re (expressões regulares)
        
        re — Regular expression operations (Expressões regulares)
        
    - beautifulsoup (extração de dados de HTML e XML)
        
        
    

Comentário em python

```python
# Adicione um '#' na frente de cada linha que desejar comentar
```